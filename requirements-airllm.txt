# AirLLM server dependencies
# Install with: pip install -r requirements-airllm.txt

# Core inference engine
airllm>=3.0.1

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Networking
httpx>=0.27.0

# Optional: required ONLY for --compression 4bit or --compression 8bit
# bitsandbytes>=0.43.0
